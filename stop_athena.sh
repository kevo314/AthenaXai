# Athena Xai v1 — TTS container dependencies
# kokoro-onnx and onnxruntime-gpu installed separately in Dockerfile
# numpy <2 force-installed separately in Dockerfile
soundfile==0.13.1
requests==2.33.0
